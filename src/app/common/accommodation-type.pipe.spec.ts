import { AccommodationTypePipe } from './accommodation-type.pipe';

describe('AccommodationTypePipe', () => {
  it('create an instance', () => {
    const pipe = new AccommodationTypePipe();
    expect(pipe).toBeTruthy();
  });
});
